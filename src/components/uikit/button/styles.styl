@import "~components/variables"

$hover = 20%
$disabled = 90%
ComposeButton($color)
  &
    background: $color
    border-color: $color

    &:hover, &.active
      background: darken($color, $hover)
      border-color: darken($color, $hover)

    &.hollow, &.fakelink
      color: $color

      &:hover
        color: darken($color, $hover)

    &.fakelink:hover
      background: transparent

    &.disabled, &.disabled:hover
      background: lighten($color, $disabled)
      border-color: lighten($color, $disabled)

.Button
  display: inline-block
  padding: 0 40px
  height: 40px
  line-height: @height
  border: 1px solid $text-grey
  background: white
  outline: none
  color: $text-grey
  border-radius: 3px
  transition: all linear 200ms
  cursor: pointer
  font: normal normal 400 20px Rokkitt, serif

  //
  // colors
  &.primary, &.success, &.warning, &.danger
    color: white

    &.hollow, &.hollow:hover
      background: white

  &.primary
    ComposeButton($button-primary)
  &.success
    ComposeButton($button-success)
  &.warning
    ComposeButton($button-warning)
  &.danger
    ComposeButton($button-danger)

  //
  // sizes
  &.small
    font-size: 12px
    padding: 0 15px
    height: 30px
    line-height: @height

  &.bold
    font-weight: 600

  //
  // behaviour
  &.disabled
    cursor: default

  &.fakelink
    border: none
    background: transparent
    text-decoration: underline
    padding: 0
    height: auto
    line-height: initial

  &.centered
    position: relative
    left: 50%
    transform: translateX(-50%)

  &.form
    margin-top: 35px
    width: 40%
    float: right
    height: 50px
    line-height: @height

    &.centered
      width: auto
      float: none

  //
  // dimensions
  &.fullwidth
    width: 100% !important

  //
  // children
  .Spinner
    vertical-align: text-bottom
